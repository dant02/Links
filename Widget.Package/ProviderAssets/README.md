# Widget picker images

Before packaging on Windows, add these PNG files:

- `HelloWorld.png` — the provider and widget icon.
- `HelloWorldScreenshot.png` — a screenshot shown by the Widgets Board picker.

Their paths are declared in `Package.appxmanifest`. The widget is not discoverable in the picker until these files are present.
